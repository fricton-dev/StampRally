# Docker Compose環境変数

# Database
DB_USER=fricton
DB_PASSWORD=fricton99
DB_NAME=stamprally-db
DB_PORT=5432

# API
API_PORT=8000

# Web
WEB_PORT=8080

# Frontend dev (optional)
VITE_DEV_HTTPS=false
# Certificates are expected under nginx/app/.cert by default.
# Use mkcert or other tooling to generate them when enabling HTTPS.
VITE_DEV_CERT_DIR=nginx/app/.cert
VITE_DEV_CERT_PATH=
VITE_DEV_KEY_PATH=
VITE_API_BASE_URL=http://localhost:8000/api
VITE_DEFAULT_TENANT_ID=takizawa
